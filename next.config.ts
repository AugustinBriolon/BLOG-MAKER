/** Configuration Next.js du POC Blog Maker (React Compiler). */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactCompiler: true,
  reactStrictMode: true,
};

export default nextConfig;
