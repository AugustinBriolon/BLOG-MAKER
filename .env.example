# AI Gateway / sujets

Copier vers `.env.local` :

```bash
AI_GATEWAY_API_KEY=
# optionnel
AI_TOPICS_MODEL=openai/gpt-4.1-nano
```

Sans clé, `/api/analyze` fonctionne ; `/api/topics` renvoie `unavailable` et l’UI affiche un état minimal.
